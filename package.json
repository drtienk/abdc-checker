{
  "name": "abdc-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "python scripts/generate_abdc_json.py",
    "predev": "npm run generate:data",
    "dev": "vite",
    "prebuild": "npm run generate:data",
    "build": "vite build",
    "preview": "vite preview",
    "generate:data": "python scripts/generate_abdc_json.py"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}